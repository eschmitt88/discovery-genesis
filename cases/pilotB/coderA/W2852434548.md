---
kind: genesis-card
id: W2852434548
codebook: v0-open
coder: sonnet-coderA
recognised: "no"
evidence: abstract-only
is_primary: "partial"
problem: "Open Information Extraction produces surface-form, non-canonical (subject, relation, object) triples that are not directly usable as RDF/Knowledge Graph facts (ambiguous entities, unnormalized relations)."
problem_age: "established (3–15 y)"
ingredients:
  - what: "Open Information Extraction (OpenIE) systems"
    status: cited
    year: 2007
    role: method
  - what: "Named entity recognition / disambiguation and entity-linking tools (AGDISTIS, NERD)"
    status: cited
    year: 2014
    role: method
  - what: "Semantic Web / RDF and Knowledge Graph target formalism"
    status: cited
    year: 2016
    role: theory
  - what: "DBpedia / Wikidata as reference knowledge bases"
    status: cited
    year: 2015
    role: data
  - what: "Semantic role labeling / frame semantics for relation typing"
    status: cited
    year: 2006
    role: method
move: "Chains OpenIE extraction together with entity linking/disambiguation and relation canonicalization into a single pipeline aimed specifically at populating a knowledge graph, rather than leaving OpenIE output as raw text triples."
move_candidates: [recombination, consolidation]
enabler: "nothing clearly identifiable as new — the paper recombines already-mature NLP/Semantic-Web components (OpenIE, entity linking, ontology alignment); no single new dataset, tool, or theory is flagged as the trigger in the abstract or reference list."
genesis_model: accretion
genesis_confidence: medium
genesis_evidence: "No abstract survives in the dossier, so the title and the shape of the (unusually large, 105-item) reference list are the main evidence: references span OpenIE (2007–2017), entity linking (2011–2016) and Semantic Web/RDF resources (2003–2017) with no single recent breakthrough cited disproportionately, consistent with an integrative/consolidating contribution rather than one built on a fresh idea or a fresh instrument."
authors_story: "Not recoverable — no abstract or full text in the dossier; the title alone ('OpenIE-based approach for Knowledge Graph construction from text') frames the contribution as a named pipeline/approach rather than a single claim."
contribution: "Proposes and (presumably) evaluates a pipeline that turns OpenIE-extracted triples into knowledge-graph-ready facts by adding entity disambiguation and relation canonicalization steps."
---

## Reasoning
With no abstract and no full text, the call rests almost entirely on the reference list's composition and the title's own framing as an "approach" built from named sub-techniques (OpenIE, entity linking, ontology infrastructure) rather than a single stated proposition. That pattern — many mature, separately-established components stitched into a working system, no isolable "before this, nobody knew X" claim visible — is the accretion signature. I'd raise confidence to high with the abstract in hand; medium reflects that this reading substitutes reference-list shape for narrative evidence.

## Alternative reading
Second-most-likely: idea-first, if the (unseen) abstract turns out to state a specific canonicalization or disambiguation technique as the paper's core proposition (e.g., "OpenIE triples can be reliably mapped to RDF via X"), in which case the genesis would be a stated idea whose design (the pipeline) follows from it. Without the abstract this can't be ruled out, hence it is kept as the runner-up rather than dismissed.

## Notes for the codebook
This is the clearest case in the batch where the codebook is starved of evidence: title + 105 undated-priority references is a weak basis for `genesis_model`, and I would flag any card built this way as low-reliability if the abstract or full text later becomes available. The `move` field also strains at "recombination" vs "consolidation" — a pipeline-style systems paper that assembles known NLP stages for a stated engineering goal doesn't map cleanly onto either; the codebook could use a dedicated `pipeline-integration` or `engineering` move label.
